<?php
// Configurações para InfinityFree - flammaindex.rf.gd
$host = 'sql303.infinityfree.com';     // Servidor MySQL do InfinityFree
$dbname = 'if0_39821261_FlammaIndex';  // Nome do banco no InfinityFree
$user = 'if0_39821261';                // Usuário MySQL do InfinityFree
$pass = '251207vi';                    // Sua senha MySQL do InfinityFree

try {
    $pdo = new PDO("mysql:host=$host;dbname=$dbname;charset=utf8mb4", $user, $pass);
    $pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
    $pdo->setAttribute(PDO::ATTR_DEFAULT_FETCH_MODE, PDO::FETCH_ASSOC);
    $pdo->setAttribute(PDO::ATTR_EMULATE_PREPARES, false);
    
    // Descomente a linha abaixo apenas para testar a conexão
    // echo "Conectado com sucesso ao InfinityFree!";
    
} catch (PDOException $e) {
    // Em produção, não exiba detalhes do erro por segurança
    if (isset($_GET['debug'])) {
        die("Erro ao conectar: " . $e->getMessage());
    } else {
        die("Erro de conexão com o banco de dados. Tente novamente mais tarde.");
    }
}

// Função auxiliar para verificar conexão
function testarConexao() {
    global $pdo;
    try {
        $stmt = $pdo->query('SELECT 1');
        return true;
    } catch (PDOException $e) {
        return false;
    }
}
?>